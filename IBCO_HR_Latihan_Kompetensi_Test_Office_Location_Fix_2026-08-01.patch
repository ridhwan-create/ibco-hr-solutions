diff --git a/tests/Feature/TrainingCompetencyManagementTest.php b/tests/Feature/TrainingCompetencyManagementTest.php
--- a/tests/Feature/TrainingCompetencyManagementTest.php
+++ b/tests/Feature/TrainingCompetencyManagementTest.php
@@ -6,6 +6,7 @@ use App\Models\CompetencyRequirement;
 use App\Models\DevelopmentPlan;
 use App\Models\EmployeeCompetency;
 use App\Models\EmployeeUserLink;
+use App\Models\OfficeLocation;
 use App\Models\TrainingApprovalAssignment;
 use App\Models\TrainingBudget;
 use App\Models\TrainingCourse;
@@ -68,6 +69,15 @@ beforeEach(function () {
         'jawatan' => 'Eksekutif Teknologi Maklumat',
         'rcd_enable' => 1,
     ]);
+    OfficeLocation::query()->create([
+        'name' => 'IBCO Solutions HQ',
+        'address' => 'Kuala Lumpur',
+        'latitude' => 3.1390000,
+        'longitude' => 101.6869000,
+        'radius_meters' => 100,
+        'accuracy_limit_meters' => 100,
+        'is_active' => true,
+    ]);
 });
 
 afterEach(function () {
@@ -87,6 +97,7 @@ test('complete training competency and development workflow keeps db_spp read on
     EmployeeUserLink::query()->create([
         'user_id' => $employee->getKey(),
         'employee_id' => 100,
+        'office_location_id' => OfficeLocation::query()->value('id'),
         'is_active' => true,
         'created_by' => $hr->getKey(),
         'updated_by' => $hr->getKey(),
@@ -284,6 +295,7 @@ test('training approval rejects costs above available departmental budget', func
     EmployeeUserLink::query()->create([
         'user_id' => $employee->getKey(),
         'employee_id' => 100,
+        'office_location_id' => OfficeLocation::query()->value('id'),
         'is_active' => true,
     ]);
     TrainingBudget::query()->create([
